{{#each ideas}}
    
    <div class="card card-body mb-2">
        <h4>{{title}}</h4>
        <p>{{details}}</p>
        <a href="/ideas/edit/{{id}}" class="btn btn-outline-dark btn-sm btn-block"> Edit </a>
    </div>
 
{{else}}
    <h2> No Ideas Here!</h2>
{{/each}}